# Exercise 2.9.6
# futval2.py
#	A program to compute the value of an investment
#	carried an input number of years into the future
def main():
	print "This program calculates the future value"
	print "of an annually recurring investment."
	
    payment = float(input("Enter annual contribution: "))
    apr = float(input("Enter the annual interest rate: "))
    years = int(input("Enter duration of investment (in years): "))

    for i in range(years):
        payment = payment * (1 + apr)
        print "The value in &s years is: &s", %(years, principal)
        payment = payment + payment

main()
